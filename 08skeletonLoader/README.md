Mostly the pseudo elements like before and after ki position needs to be decided based on their main element
so declare the pseudo element ki position as absolute and the main element position as relative

Pseudo element me we need to write pass content. If it is empty write ""

To prevent the shimmer effect from leaving that div, we need to specify in that parent div
```css
overflow:hidden;
```

